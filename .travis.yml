language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-frank' ]; then cd .. && eval "mv $currentfolder generator-frank" && cd generator-frank; fi

